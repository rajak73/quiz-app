# Netlify Deployment Configuration
[build]
  publish = "frontend"
  command = "echo 'No build step required for static HTML'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
